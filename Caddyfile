# Caddy Reverse Proxy

:80 {
    handle /api/* {
        reverse_proxy api:3000 {
            transport http {
                versions h2c
            }
        }
    }
    
    handle {
        respond "God-Stack API" 200
    }
    
    log {
        output stdout
        format json
    }
}

# Production: Uncomment and configure
# api.example.com {
#     reverse_proxy api:3000
#     encode gzip zstd
#     header {
#         Strict-Transport-Security "max-age=31536000"
#         X-Content-Type-Options nosniff
#         X-Frame-Options DENY
#     }
# }
